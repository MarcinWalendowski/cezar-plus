# Brief — this run IS the disposable test fixture; there is no feature to spec

**Task id:** `a3dd8f5f-5d66-402e-b876-c1a6746d9da7` · **Step:** 1/9 — Gather the record (brief
only; no spec or code written here) · **Date:** 2026-08-25

## Headline finding — read this before doing anything else

The task title, `"E2E disposable: 480e0282 #2"`, is not a feature request. It is the **literal,
verbatim title of a disposable todo** that task `480e0282-a967-4936-a12e-3c4e56450586`'s own
spec required it to create as throwaway fixture data for a browser E2E test, and this run
(`a3dd8f5f`) is the **real cezar task that starting that todo spawned**. There is no product,
bug or design decision behind this title to discover — the "record" for this task IS the record
of how it was created, and the correct outcome is very likely to cancel/delete this run as part
of that E2E's own cleanup, not to write a spec and ship code.

**Do not write a spec proposing a feature for this title in step 2.** If this brief is being read
because the run is still alive, the open question for the next step is procedural (should this
run stop itself, wait, or proceed) — see § Open questions.

## The problem, in this repository's own terms

`cezar` is a parallel coding-agent orchestrator. Its `/tasks` "Filed" board lets a user select
multiple filed todos and click **"Run N tasks"**, which `POST`s `/todos/:id/start` for each
selection and creates one real cezar task/run per todo (feature: "bulk start filed tasks",
shipped commit `7932cf4d83ff6a4f263ae7181ec0d8e9fa81ea7f`, now on `origin/main` and live-deployed
— see § What the record already decided).

Task `480e0282` ("Ship bulk start filed tasks") is the task that built, gated, committed, pushed
and deployed that feature, and its own spec's Phase 5 (P5, "Real browser E2E on production")
requires it to prove the feature works by driving a real authenticated Chromium session against
the live cockpit at `http://127.0.0.1:4321`:

1. Create two disposable todos via `cezar todo add "E2E disposable: 480e0282 #1" --project cezar`
   and `#2` (ship spec, `.ai/specs/2026-08-24-ship-bulk-start-filed-tasks.md:868`).
2. Tick both rows on the Filed table and click **Run 2 tasks**.
3. Assert two `201` responses from `/todos/:id/start`, capture the two new run ids, assert the
   two rows disappear from Filed.
4. **In a `finally`: cancel both captured runs, delete both runs, delete both todo ids
   unconditionally, assert ground truth, destroy the E2E session** (ship spec:912-956).

**Measured directly, not inferred:**

- `cezar/.ai/cezar/todos.json` — both todos exist, created `2026-08-25T08:18:36Z`/`:36.591Z` by
  author `{"kind":"agent","id":"480e0282-…","parentTaskId":"480e0282-…","parentStepId":"continue-3"}`,
  `summary` fields exactly `"E2E disposable: 480e0282 #1"` and `"#2"`, both carrying a
  `tombstone.at: 2026-08-25T08:18:56Z` — 20 seconds after creation, which lines up with "started
  todos are hidden by `visibleTodos()`" (ship spec:911), not a delete.
- `cezar/.ai/cezar/runs/a3dd8f5f-5d66-402e-b876-c1a6746d9da7.handoff.md` (this run):
  `# Handoff — 2: E2E disposable: 480e0282 #2`, Goal = `E2E disposable: 480e0282 #2`, no context,
  no acceptance criteria — a bare title, exactly what `POST /todos/:id/start` would produce from
  a todo whose only field is `summary`.
- `cezar/.ai/cezar/runs/ae7bd42f-a399-4ceb-92cf-d657e620d80f.handoff.md` — the sibling: **the
  same shape**, `# Handoff — 1: E2E disposable: 480e0282 #1`, same workflow (`spec-to-deploy`),
  same 324-byte size (i.e. also still at step 1, nothing has run yet).
- This run's own `lifecycle: run started` fired `2026-08-25T08:18:40.715Z`
  (`cezar/.ai/cezar/runs/a3dd8f5f-….ndjson:2`) — 4 seconds after todo `#2` was created and 16
  seconds before it was tombstoned. The timeline is consistent with: create todo → click Run 2
  tasks → `/start` spawns this task → board hides the row.
- **An earlier cleanup attempt already failed once**, inside `480e0282`'s own run, step
  `continue-3` (`cezar/.ai/cezar/runs/480e0282-….ndjson`, tool result timestamped
  `2026-08-25T08:20:01.644Z`, `isError:true`, `exitCode:1`). Its script queried
  `/api/v1/workspace/runs-index` and `/api/v1/workspace/todos`, found **zero matching runs**
  (`"foundRuns":[]`) but **both** disposable todo ids as `foundTodos`, then issued
  `DELETE /api/v1/p/cezar/todos/:id` for each and got **`404 {"error":"not found"}`** for both,
  then re-queried and still found both todos present, and reported
  `"failures":["remaining runs=0 todos=2"]`. In other words: the cleanup script ran, but at the
  point it ran it either could not see the two runs it needed to cancel (this one and `ae7bd42f`)
  or was querying the wrong thing, and its todo-delete calls 404'd — plausibly because a
  **started/tombstoned todo is not a target `DELETE /todos/:id` recognizes**, and the correct
  cleanup for a started todo is to cancel+delete the *run* it spawned, which restores the todo
  first (see next point), not to delete the todo directly.
- `cez kb search` confirms the mechanism: cancelling a run **un-hides** the todo it was started
  from (`clearStartedTaskId`, `packages/cezar/src/server/server.ts:5199-5206`; KB
  `specs-ec00b75ebcfc` "Cancelling a run un-hides its originating todo"; changelog
  `notion-c6c2d0e8f3e8`, 2026-08-22). So the ship spec's own cleanup order (cancel run → delete
  run → delete todo unconditionally, ship spec:912-932) exists precisely because cancel puts the
  todo back on the board first.
- **`480e0282` was mid-cleanup when the cezar service itself restarted.** Both this run's and
  `480e0282`'s own ndjson logs carry `"cezar restarted — this run kept going"` at
  `2026-08-25T08:20:52.843Z`/`08:21:07.593Z`, and `480e0282` then re-entered a **fresh** step
  named `context` / "Gather the record" at `08:21:07.874Z` — i.e. it did not resume mid-cleanup,
  it started over from the top of a new continuation. Its handoff's own last-recorded progress
  line is still the pre-restart failed cleanup; nothing in the record shows the cleanup has
  succeeded since.

## What the record already decided (with citations)

- **The feature this todo pair tests is real, shipped, and live.** Commit `7932cf4d` (`feat:
  bulk start filed tasks`) is on `origin/main` (confirmed: `git merge-base --is-ancestor 7932cf4d
  origin/main` → yes; fetched `origin/main` = `d217ab2e…`, which contains `7932cf4d` in its
  history) and it is the **currently live** deploy: `GET /api/v1/ready` on this box reports
  `"deploy":{"releaseId":"20260825T081138Z-00a202b8","sha":"00a202b8…"}`, and `00a202b8` is a
  descendant of `7932cf4d` in `git log --oneline origin/main`. So P4 (deploy) of `480e0282`'s own
  spec is effectively already satisfied by ordinary downstream traffic on `main` — it does not
  need to be redone.
- **The E2E procedure that created this run is fully specified**, not improvised:
  `.ai/specs/2026-08-24-ship-bulk-start-filed-tasks.md` §P5 (`origin/main`, lines 803-959) and
  §P6 (961-1076), reproduced in relevant part above. It states its own risk explicitly: *"The E2E
  leaves stray rows, runs or a live session behind… cancel restores the todo rather than
  consuming it… a minted session outlives the process that made it"* (Risks table, ship
  spec:1095) — this run and its sibling are exactly that risk materialised.
- **This is not the first time cleanup for this exact feature's E2E has gone wrong.** The same
  todos.json carries an earlier, separate pair of E2E artifacts from a prior attempt (todo
  `hubSeq: 268`, `startedOn: "06495ac4-0146-4e6d-b021-ea591f3cc63e"`), i.e. today's run is at
  least the second time real disposable runs have been left running against production from this
  same E2E phase.
- **Standing workspace rule: never leave a task's goal unaddressed by inventing scope.** This
  repo's own `AGENTS.md` (`cezar/AGENTS.md`, "Changing a mechanism that already works" and
  "Zero config" sections quoted in the system prompt) and the workspace `CLAUDE.md` both push
  toward *reading what already exists* before acting and toward *not adding scope the record
  doesn't call for*. Writing a feature spec for a todo whose entire content is a disposability
  marker would be exactly that: invented scope with no requester behind it.

## Which code is actually involved

- `packages/cezar/src/server/server.ts` — `POST /todos/:id/start` (creates the run, 201, ship
  spec:902), `POST /api/v1/runs/:id/cancel` (`clearStartedTaskId`, :5199-5206), `DELETE
  /api/v1/runs/:id` (409 while active, :5879-5881), `DELETE /api/v1/p/:projectId/todos/:id`
  (:6144, "ungated since D7a" per ship spec:930).
- `packages/web/src/routes/global-tasks.tsx` / `packages/web/src/lib/filed-tasks.ts` — the Filed
  table, multi-select, `selectedFiledEntries`, the `Run N tasks` button — the feature under test,
  already shipped (§ above), **not** something this task needs to touch.
- `cezar/.ai/cezar/todos.json` — the two disposable todo records (ids `2d0b837a-…` = `#1`,
  `520e2bbe-…` = `#2`), both tombstoned, neither yet deleted.
- This run's own worktree, `cezar/.ai/cezar/worktrees/a3dd8f5f-5d66-402e-b876-c1a6746d9da7`, and
  its sibling `…/ae7bd42f-a399-4ceb-92cf-d657e620d80f` — both real, both currently on step 1 of a
  9-step `spec-to-deploy` chain, both consuming a `maxParallel` slot and real tokens for a title
  that has no product intent behind it.

## Any prior decision this would contradict

Proceeding to step 2 and writing a real feature spec for "E2E disposable: 480e0282 #2" would
contradict the ship spec's own explicit intent for these todos — they exist **only** to be
clicked, captured, and then cancelled/deleted within `480e0282`'s own P5/P6 (ship spec:912-956).
Building anything under this title also has no requester: it was not typed by the owner, it was
generated by an E2E fixture step. Spending a 9-step chain (spec → review → implement → gates →
commit → deploy → E2E → document) on it would itself recreate the exact "stray disposable run"
risk the parent spec's own Risks table warns about, one level higher.

## What this brief could NOT verify (open items for later steps)

- **Whether `480e0282`'s restarted continuation will still complete cleanup on its own.** Its
  ndjson shows it re-entered "Gather the record" at `08:21:07Z`; whether that continuation's next
  turns re-derive "cancel + delete `a3dd8f5f` and `ae7bd42f`" or take a different path is unknown
  from the record alone — it is a live, concurrently-running task this brief cannot predict.
- **Why the `continue-3` cleanup script's `runs-index` query returned zero matching runs** when
  (per the timeline) both `a3dd8f5f` and `ae7bd42f` should have already existed. Possibly a
  filter/regex mismatch (the script strips a leading `"^\d+: "` from `r.title`, and this run's
  handoff title carries a `"2: "` prefix — worth checking whether the live API's `run.title` also
  carries that prefix and whether the strip regex actually matches it), possibly an indexing
  delay. Not confirmed either way from the logs alone.
- **Whether there is a supported, safe way for a running task to cancel *itself*** (via
  `TaskStop`, `cezar todo`/`run` CLI, or otherwise) versus needing the *parent* task or the owner
  to do it from outside. Not found in the record read so far.
- **The exact API shape of `/api/v1/runs/:id/cancel` and `DELETE /api/v1/runs/:id` from the
  caller's side** (auth requirements when called from inside a task rather than a minted E2E
  session) — cited above from the ship spec's description, not independently re-verified against
  the route source in this pass.

## Open questions the next step has to settle

1. Should step 2 skip spec-writing entirely and instead have this run request/perform its own
   cancellation (report back "this is disposable test fixture data, self-terminating" rather than
   producing `.ai/specs/2026-08-25-*.md`)? This brief's reading of the record says **yes**, but
   that is a process decision for the next step, not this one.
2. If self-cancellation is not possible or not appropriate from inside the run, what is the
   minimal safe action — end the chain cleanly with no code changes and say so, or wait/poll for
   `480e0282` to finish its own cleanup?
3. Should this brief's finding (a second E2E cleanup failure for the same feature, two runs
   deep) be filed as a **real** follow-up bug against `480e0282`'s cleanup procedure — e.g. "the
   cleanup script's runs-index lookup missed both spawned runs" — separately from whatever this
   run itself does? That would be a legitimate small spec/todo, distinct from treating this run's
   own title as a feature to build.

## What this brief did NOT do

No sub-agents were fanned out for this brief. The three "gather" axes (record, code, in-flight
duplicate work) were answered directly and conclusively from primary sources (todos.json, run
ndjson/handoff files, the committed ship spec, live `git`/`curl` state) with exact citations
above; a parallel investigation would have re-derived the same facts rather than adding new ones,
which the task instructions call out as waste. No spec, code, or test was written.
